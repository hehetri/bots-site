document.addEventListener('DOMContentLoaded',()=>{const time=document.querySelector('#server-time');function updateTime(){const d=new Date();const f=new Intl.DateTimeFormat('en-GB',{day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',hour12:false,timeZoneName:'short'});time.textContent=f.format(d).replace('GMT','UTC')}updateTime();setInterval(updateTime,30000);document.querySelectorAll('.nav-item').forEach(a=>a.addEventListener('click',()=>{document.querySelectorAll('.nav-item').forEach(n=>n.classList.remove('active'));a.classList.add('active')}));});